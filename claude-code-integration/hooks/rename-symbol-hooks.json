{
  "description": "Claude Code hooks for the ghidra-function-port pipeline. Add these to your project's .claude/settings.local.json under the 'hooks' key.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__ghidra__rename_symbol",
        "hooks": [
          {
            "type": "command",
            "command": "echo '⚠️ FUNCTION PORT PIPELINE: Before renaming in 0762_Base.hex, confirm: (1) Match was found via search_bytes signature, (2) Confidence score was calculated, (3) This rename is part of the ghidra-function-port pipeline. If not, invoke the ghidra-function-port skill first.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__ghidra__rename_symbol",
        "hooks": [
          {
            "type": "command",
            "command": "echo '📋 ANNOTATION CHECKLIST: After rename, complete ALL remaining steps: (1) set_function_prototype, (2) rename ALL globals (Ve*/Ke*/Vb*), (3) create structs if needed, (4) set_local_variable_type, (5) set_comment with plate comment, (6) VERIFY by re-decompiling. Do NOT move to next function until all steps complete.'"
          }
        ]
      }
    ]
  }
}
