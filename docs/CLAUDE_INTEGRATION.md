# Claude Code integration

The versioned `ghidrassist` plugin is under [`claude-code-integration/`](../claude-code-integration/). Install it through [`.claude-plugin/marketplace.json`](../.claude-plugin/marketplace.json); its HTTP server configuration preserves the `ghidrassist` server key.

The package provides `port-functions` and `deep-function`. Both load plugin-local operating-guide references and use the server's advertised schemas. Positional arguments accept whitespace-free project paths, resolved once to exact program IDs; ambiguous paths require explicit selection. For example, `/ghidrassist:port-functions /source.bin /target.bin selected-functions`.

Run `syncOperatingGuides` after changing the canonical server guide; `checkOperatingGuides` rejects drift and is included in `check` and `buildExtension`. Plugin structure and argument substitution follow the [official plugin reference](https://code.claude.com/docs/en/plugins-reference) and [skill documentation](https://code.claude.com/docs/en/skills). An isolated `--plugin-dir` smoke with Claude Code 2.1.269 invoked the expected `mcp__ghidrassist__runtime_capabilities` and `mcp__ghidrassist__list_binaries` prefixes. The separately tested legacy 8080 runtime rejected new optional discovery arguments; the fixture trace below is the current contract evidence.

The first disposable discovery smoke accepted `include_programs=false` and `limit=1`, returned a SHA-256 inventory cursor, preserved the `space # selector` ID, and used the target's exact opaque ID. The round-two runtime-capabilities call was intentionally omitted because that fixture predates the fixed schema. The captured mutation trace is `C:\Users\JResp\AppData\Local\Temp\ga-client-smoke\round2-claude-561f35db30af4c1a974dfe4988b08d32.jsonl` (session `c8c83472-feff-46c1-aff4-5daebfb4a1ac`): `set_function_prototype(return_code=true)` committed and verified `int fixture_probe(int value)`, async save completed, and reinspection after client close/reacquisition confirmed the stored prototype. Save advanced the target ID from `#version=1` to `#version=2`; the trace correctly rediscovered it. The fixture's close result released the MCP consumer while `object_closed=false`, so this is client reacquisition evidence rather than a claim of a fresh JVM object.

The packaged fixture also completed an explicit PORT smoke using Claude Code. Trace: `C:\Users\JResp\AppData\Local\Temp\claude-packaged-port-replace.json`. With `name_policy=replace` and `signature_policy=replace`, preview/apply recorded `claude-packaged-port-replace-v1`; `port_ledger verify` returned `verified=true`, save completed, and the ledger remained verified after close/reopen and ID rediscovery. The source fingerprint remained unchanged.
