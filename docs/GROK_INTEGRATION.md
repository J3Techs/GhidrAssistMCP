# Grok integration

[`grok-integration/config.example.toml`](../grok-integration/config.example.toml) is an inactive fragment using Grok's documented `[mcp_servers.ghidrassist]` HTTP registration and `[mcp] max_output_bytes = 131072` cap. Apply it only after checking the installed client and endpoint.

Keep project configuration inactive until precedence is verified. Use `search_tool` → `use_tool`, canonical family searches, exact program IDs, and live schemas. Matchers and transfer contracts are not claimed available by this package merely because the workflow is documented.

An isolated Grok 1.0.30 headless smoke was run from the trusted repository child `build/grok-client-smoke`, with managed MCPs disabled through process environment variables. `grok inspect --json` showed the project-scoped `ghidrassist` HTTP server; no global configuration or credentials were copied. The read-only trace used `list_binaries(limit=10)` and `get_function_info` with the exact target ID and address `00001000`.

Read-only inspection found Grok 1.0.30 (build `04b7ffed98c6`) uses `[mcp_servers.<name>]`; its local documentation supports project configuration and `[mcp] max_output_bytes`. The packaged-JAR fixture smoke used endpoint `http://127.0.0.1:55009/mcp` and trace `C:\Users\JResp\AppData\Local\Temp\grok-packaged-mutation.json` (session `01a093c5-b752-7ea2-a363-feb804ba39d3`). Grok successfully called `set_function_prototype(return_code=true)`, verified `int fixture_probe(int value)`, and awaited async `save_program` completion (`saved=true`). A follow-up trace, `C:\Users\JResp\AppData\Local\Temp\grok-packaged-reopen.json` (session `01a093c7-e274-72a0-b03c-450186d302f1`), closed the exact `#version=2` ID (consumer release, `object_closed=false`), reopened `/target`, rediscovered the current ID, and confirmed the stored prototype at `00001000`. The first trace's attempted close with stale `#version=1` was correctly rejected after save revision advancement. No runtime capabilities call or file edit was made.

Grok then read the PORT checkpoint written by Claude without mutation. Trace: `C:\Users\JResp\AppData\Local\Temp\grok-packaged-port-ledger.json` (session `01a093ce-49d0-7e91-99c5-5a6dfb15e1cd`). `list_binaries(limit=8)` supplied the exact target `#version=3` ID; `port_ledger get` at `00001000` returned `verification=verified`, `checkpoint_matches=true`, operation `claude-packaged-port-replace-v1`, and a destination fingerprint matching `current_fingerprint`.
